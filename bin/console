#!/usr/bin/env ruby
# frozen_string_literal: true

$DEBUG = ENV.fetch('DEBUG', 'false') == 'true'
$VERBOSE = ENV.fetch('VERBOSE', 'false') == 'true'

require 'configurist'

require 'irb'
IRB.start(__FILE__)
